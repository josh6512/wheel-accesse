NODE_ENV=development
PORT=3000
CLIENT_ORIGIN=http://localhost:5173

# Replace every placeholder locally. Wrap reserved characters in credentials with {braces}.
DATABASE_URL="sqlserver://localhost:1433;database=wheel_accesses_dev;user=YOUR_USERNAME;password=YOUR_PASSWORD;encrypt=true;trustServerCertificate=true"
